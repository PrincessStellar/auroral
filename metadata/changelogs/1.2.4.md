# Auroral 1.2.4

## Hotfix

- Aurora Bloom placement and Glacial Basin filling now use only the `c:is_cold` biome tag as their criterion, replacing the stricter `IS_TAIGA`/`IS_MOUNTAIN`/`coldEnoughToSnow` checks that excluded many valid cold biomes
